import React from "react";
const filmesView = ({ match }) => {
  var teste = 1;
  if (!teste) {
    return <div>Movie not found</div>;
  }

  return (
    <div>
      <h1>Mais um teste</h1>
    </div>
  );
};

export default filmesView;
